# Database Configuration
DATABASE_URL=postgresql://username:password@host:port/database_name

# Server Configuration
PORT=3000

# JWT Secret (add this later if needed)
JWT_SECRET=your_jwt_secret_here
